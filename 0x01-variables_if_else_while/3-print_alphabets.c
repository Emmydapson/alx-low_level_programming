#include <stdio.h>
/**
 * main - Entry point
 * Return: always 0
 */

int main(void)
{
	char n;
	char c;

	n = 'a';
	c = 'A';
	while
		(n <= 'z') {
		putchar(n);
		n++;
		}
	while
		(c <= 'Z') {
		putchar(c);
		c++;
		}
	putchar('\n');
	return (0);
}
